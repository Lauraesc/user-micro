
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/foodcourt_user
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

  logging:
    level:
      org:
        springframework:
          orm:
            jpa: DEBUG
  spring:
    cloud:
      compatibility-verifier:
        enabled=false